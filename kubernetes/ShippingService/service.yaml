apiVersion: v1
kind: Service
metadata:
  namespace: eshop-eda
  labels:
    app: shipment-service
  name: shipment-service
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    name: shipment-backend