apiVersion: v1
kind: Service
metadata:
  namespace: eshop-eda
  labels:
    app: order-service
  name: order-service
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    name: order-backend