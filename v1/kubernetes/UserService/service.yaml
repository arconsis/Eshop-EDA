apiVersion: v1
kind: Service
metadata:
  namespace: eshop-eda
  labels:
    app: user-service
  name: user-service
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: user-app