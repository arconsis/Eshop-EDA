apiVersion: v1
kind: Service
metadata:
  namespace: eshop-eda
  labels:
    app: user-service
  name: user-service
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    name: user-backend