apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-app
  labels:
    app: user-app
  namespace: eshop-eda
spec:
  replicas: 1
  selector:
    matchLabels:
      name: user-app
  template:
    metadata:
      labels:
        name: user-app
    spec:
      containers:
      - name: user-eda
        imagePullPolicy: Always
        image: eldimious/user-eda-v1:latest
        ports:
          - containerPort: 8080
            protocol: TCP
        resources:
          limits:
            cpu: '200m'
            memory: '512Mi'
          requests:
            cpu: '200m'
            memory: '512Mi'
        envFrom:
        - secretRef:
            name: users-service-secrets
            