apiVersion: v1
kind: Service
metadata:
  namespace: eshop-eda
  labels:
    app: email-service
  name: email-service
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    name: email-backend