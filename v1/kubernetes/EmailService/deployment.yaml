apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-backend
  labels:
    app: email-backend
  namespace: eshop-eda
spec:
  replicas: 1
  selector:
    matchLabels:
      name: email-backend
  template:
    metadata:
      labels:
        name: email-backend
    spec:
      containers:
      - name: email-api
        imagePullPolicy: Always
        image: eldimious/email-eda-v1:latest
        envFrom:
        - secretRef:
            name: email-service-secrets
        resources:
          limits:
            cpu: '500m'
            memory: '512Mi'
          requests:
            cpu: '500m'
            memory: '512Mi'
        ports:
          - containerPort: 3000
            protocol: TCP       